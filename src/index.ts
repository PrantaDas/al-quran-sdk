/**
 * al-quran-sdk
 *
 * TypeScript SDK for the quran.com v4 REST API. The package exposes the
 * grouped API namespaces (`audio`, `chapter`, `juz`, `quran`, `resources`,
 * `verse`) alongside every public type used in their signatures.
 *
 * Re-export only — every implementation lives in `./apis/*` and `./interfaces`.
 */
export * from './apis';
export * from './interfaces';
